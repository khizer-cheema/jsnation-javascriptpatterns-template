/* MVVM -> Model - View - ViewModel
Model → Stores the to-do list data.
ViewModel → Handles all UI logic (event listeners, updating DOM).
View (HTML) → Pure markup, automatically updated when ViewModel changes.

🔑 Difference from MVC:
-In MVC, the Controller updates both Model and View explicitly.
-In MVVM, the ViewModel acts as a mediator and binds the Model to the View.
-View ↔ ViewModel communicate directly via bindings/events.
*/
